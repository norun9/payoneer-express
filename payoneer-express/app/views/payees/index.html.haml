%p#notice= notice
%p#alert= alert
%h1 Listing Payees
%table
  %thead
    %tr
      %th Email
      %th Sign up url
      %th Balance
      %th Currency
      %th{:colspan => "3"}
  %tbody
    - @payees.each do |payee|
      %tr
        %td= payee.email
        %td= signed?(payee)
        %td= payee.balance
        %td= payee.currency.code
        %td= link_to 'Show', payee
        %td= link_to 'Edit', edit_payee_path(payee)
        %td= link_to 'Destroy', payee, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Payee', new_payee_path
